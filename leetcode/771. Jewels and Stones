class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        jset = set(jewels)
        count = 0

        for stone in stones:
            if stone in jset:
                count += 1
        
        return count
